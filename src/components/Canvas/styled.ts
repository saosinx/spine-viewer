import styled from 'styled-components/macro'

const CanvasContainer = styled.div`
	position: relative;
	height: 100vh;
`

const Canvas = styled.canvas`
	background: #f00;
	width: 100%;
	height: 100%;
`

export { CanvasContainer, Canvas }
